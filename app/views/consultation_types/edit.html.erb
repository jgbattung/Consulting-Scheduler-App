<div class="max-w-3xl mx-auto px-4">
<div class="flex items-center justify-between">
  <h1 class="font-bold text-3xl mb-6">Edit Consultation</h1>
  <% if current_user.consultation_link? %>
    <%= link_to "View personalized link", user_path(consultation_link: current_user.consultation_link), class:"text-gray-500" %>
  <% end %>
</div>

  <%= render 'form', consultation_type: @consultation_type %>
</div>
